# Ooustream 5.0 — plan (2026-09-24)

Scope chosen by the user: all four groups below + the unreleased larrydaw fixes (watchdog trusts
mt8696 HW, rebuilds keep FFmpeg audio, dead-provider-host message). No 4.2.19 — everything ships as 5.0.
Order = lowest risk first; Home rebuild last with its own test pass.

## A. Quick polish
- [ ] Continue Watching card: series name once, episode line = "S3 E3 · <TMDB name>", time left once;
      drop the duplicated S/E badge text; fallback art when the icon is blank
- [ ] Home hero: title + Play / More Info stay visible while the trailer plays
- [ ] Live TV category rows: fill the count badge or hide it (currently an empty grey pill on every row)

## B. Movie details redesign (TV, runtime isTV() layout like the series screen)
- [ ] Left: title, year / genre / rating / runtime, plot, Play or Resume (+ Start over), Trailer
- [ ] Right / below: cast line, "More like this" row
- [ ] Phone layout untouched

## C. Catch-up (provider tv_archive — 235 channels, ~12-13h archived as of 2026-09-24 15:55 ET)
Facts verified against bp-v2.net:
- `get_live_streams` carries `tv_archive` (0/1) + `tv_archive_duration` (days)
- `get_simple_data_table&stream_id=N` lists past programmes with `has_archive=1`
- Replay URL: `/timeshift/<user>/<pass>/<durationMin>/<YYYY-MM-DD:HH-MM>/<streamId>.ts` → 206 video/mp2t
- Listing times are UTC (server time), NOT device local — build the URL from the UTC start
- [ ] Data: `get_simple_data_table` endpoint + `StreamUrlBuilder.timeshift()`
- [ ] TV Guide: show archived past programmes for catch-up channels; OK on one = play from its start
- [ ] Player: catch-up plays as VOD-like (seekable, no live-edge logic, no binge/next)
- [ ] Channel marker for catch-up channels (Live TV list + guide)

## D. Home memory rebuild
- [ ] Measure first: RSS on Home before (baseline ~420MB after a full Home exercise per v4.2.10 notes)
- [ ] Replace the NestedScrollView of ~15 HorizontalGridViews with one recycling vertical list
- [ ] Keep: focus restore, hero, row order, genre rows, Pick Up & New
- [ ] Measure after; D-pad walk every row

## Release
- [ ] versionName 5.0.0 / versionCode 107, update.json changelog for customers
- [ ] .84 full walk; .82 install; Ooustick if on the network (else flagged unverified)

## Results (2026-09-24)
- A: done, device-verified on .84 (screenshots).
- B: done, device-verified (layout, scroll to "More like this", open similar, Back resets scroll).
- C: done, device-verified (list, day grouping, playback from 0:00, UTC timing proven by the
  on-screen station clock). First RIGHT while a channel's shows are still loading does nothing —
  the list is hidden during load on purpose (never land on the previous channel's shows).
- D: measured, NOT changed — images dominate (~145-167MB graphics); trailer off = no change;
  Precision.EXACT = worse; cache 8% = no change. Reverted. Details in CLAUDE.md v5.0.0 entry.
