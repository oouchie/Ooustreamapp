<?xml version="1.0" encoding="utf-8"?>
<!-- "More like this" poster on the TV movie page. Title only when there's no artwork. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="120dp"
    android:layout_height="180dp"
    android:layout_marginEnd="14dp"
    android:background="@drawable/sd_poster_bg"
    android:foreground="@drawable/sd_poster_focus"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <TextView
        android:id="@+id/ml_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:padding="10dp"
        android:gravity="center"
        android:textColor="@color/sd_text_secondary"
        android:textSize="14sp"
        android:maxLines="4"
        android:ellipsize="end"
        tools:text="Apex" />

    <ImageView
        android:id="@+id/ml_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@null" />
</FrameLayout>
